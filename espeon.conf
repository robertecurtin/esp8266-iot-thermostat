{
  -- Set to true to flash an ESP32 instead of an ESP8266
  esp32 = false,
  firmware = 'firmware.bin',

  -- Paths to all Lua files to flash
  source = {
    'init.lua',
    'src/*',
    'src/wifi_configuration.lua'
  },
}
